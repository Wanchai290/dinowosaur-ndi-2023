export class Sprite {
    filePath: string; // path to the sprite file
    image: ImageBitmap //TODO: is good type ?
    size: [number, number] // Express size in pixels

    constructor(filePath: string) {
        // load the sprite into image attribute
    }
}